import { BigNumber } from '@ethersproject/bignumber';
export declare const guessTitle: ({ data, from, to, chainId, value }: Partial<{
    data: string;
    chainId: number;
}> & {
    from: string;
    to: string;
    value: BigNumber;
}) => string;
