/// <reference types="react" />
import { Web3Provider } from '@ethersproject/providers';
import type { UseWalletModalOptions } from './types';
import { Web3ReactContextInterface } from '@web3-react/core/dist/types';
export declare type WalletInterface = Omit<Web3ReactContextInterface<Web3Provider>, 'activate' | 'deactivate' | 'library' | 'account' | 'active'> & {
    Modal?: () => JSX.Element;
    provider: Web3Provider | undefined;
    address: string | undefined | null;
    state: {
        connect: () => void;
        disconnect: () => void;
        isConnected: boolean;
        isConnecting: boolean;
    };
};
export declare const useWalletModal: ({ modal: ModalComponent, chains, wallets: selectedWallets, terms }: UseWalletModalOptions) => WalletInterface;
