/// <reference types="react" />
import type { ModalProps } from '../types';
/**
 * Rainbow-styled Modal
 */
export declare const Modal: ({ wallets, connect, setConnecting, isConnecting, terms, classNames }: ModalProps) => JSX.Element;
