import { AbstractConnector } from '@web3-react/abstract-connector';
import { Wallet } from '@rainbow-me/kit-utils';
/**
 *
 * @param mod in PascalCase
 * @returns
 */
export declare const importConnector: (mod: string) => Promise<any>;
/**
 * Imports and creates a connector with given options
 */
export declare const createConnector: ({ name, options, chains, connectorName }: Wallet & {
    chains?: (string | number)[];
}) => Promise<{
    instance: AbstractConnector;
    name: string;
}>;
